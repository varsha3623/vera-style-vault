name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Use Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 20
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Run type checks
        run: npm run build -- --emptyOutDir

      - name: Run lint
        run: npm run lint

      - name: Run tests
        run: npm test

      - name: Verify formatting and static files
        run: |
          if [ -f package-lock.json ]; then npm audit --audit-level=moderate; fi
